# Populated by `npx convex dev` — see README "Getting started"
CONVEX_DEPLOYMENT=
VITE_CONVEX_URL=

# Convex HTTP actions origin: same deployment as VITE_CONVEX_URL, .site not .cloud
VITE_CONVEX_SITE_URL=

# Origin this app is served from; must match SITE_URL set on the Convex deployment
VITE_SITE_URL=http://localhost:3000
